server:
  port: 8081

limit:
  minMoney: 0.01
  maxMoney: 100
  description: Money range limitation. Max ${limit.maxMoney}, Min ${limit.minMoney}

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true